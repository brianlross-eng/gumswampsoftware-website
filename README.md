# Gum Swamp Software — Company Website

Single-page website for gumswampsoftware.com, deployed via Cloudflare Pages.

## Deployment

Automatically deployed to Cloudflare Pages on every push to `main`.

## Development

Edit `index.html` and `assets/style.css`. Push to `main` to deploy.
